#include <stdio.h>

int main() {
  int n;
  printf("enter n:");
  scanf("%d",&n);
  int a=n;
for(int i=1;i<=n;i++){
 for(int j=1;j<=a;j++)printf(" ");
   a=(a-1);
   for(int k=1;k<=i;k++)printf("*");
   printf("\n");//for line change 
 }
 return 0;
}
